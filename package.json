{
  "name": "company-website",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "dev:backend": "npm run dev --workspace backend",
    "build": "npm run build --workspace frontend",
    "lint": "npm run lint --workspaces",
    "format": "prettier . --write",
    "prepare": "node -e \"if (process.env.NODE_ENV !== 'production') require('child_process').spawnSync('npx', ['husky'], { stdio: 'inherit', shell: true })\""
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.2.10",
    "prettier": "^3.3.3",
    "vite-plugin-sitemap": "^0.8.2"
  },
  "lint-staged": {
    "*.{js,jsx,css,md,json}": [
      "prettier --write"
    ]
  }
}
