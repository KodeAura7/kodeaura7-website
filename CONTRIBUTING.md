# Contributing

1. Branch from `develop` using `feature/*`, `bugfix/*`, or `hotfix/*`.
2. Keep UI changes aligned with `docs/reference/design-files/`.
3. Run lint and build before opening a pull request.
4. Never commit `.env` files or secrets.
5. Use clear commit messages and keep pull requests focused.
